import { TopicTagsPipe } from './topic-tags.pipe';

describe('TopicTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new TopicTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
